## 06/02/2026

## lab 1-1

i've personally evaluated my source code and can claim that it is up to standard. below is the application of best coding practices i've applied throughout my code:

### {clean coding}
1. meaningful names: i've picked a short and concise name for all my functions, variables, classes and packages such that one can tell what each one does at a glance without any comments present.
2. elementary functions: each functions within my source code do one thing only, relying on another functions or class to do repeating task. which is important for the scalability of the code
3. meaningful comments: the project is small enough as it is such that comment is unnecessary.
4. error handling:  there's no functions exist within the source code(at the time of writing) which handles error. however, this is insignificant when the size of the porject is currently so small.
5. object & data structure: this too, can be safely ignored as there's only one object and one data structure within the project as of yet. even then, both did follow each coding practices (that is objects should expose behavior and hide data (abstraction), while data structures should expose data and have no behavior)

### {secure coding}
it has come to my attention that none of the guidelines of authorization, authentication, input data validation nor session management have been utilized by the source code. however, most of this is insignificant as the project as of now is still in local development.


